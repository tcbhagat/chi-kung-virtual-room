services:
  - type: web
    name: chi-kung-virtual-room
    runtime: node
    plan: free
    buildCommand: npm ci --omit=dev
    startCommand: npm run room
    healthCheckPath: /health
    autoDeployTrigger: commit
    envVars:
      - key: NODE_VERSION
        value: 22
